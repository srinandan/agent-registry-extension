prompt = """
Fetch the list of Google Cloud Agent Registry agents.
First, check if the `agentregistry` MCP server is available.
If the `agentregistry` MCP server is available, use the native `list_agents` MCP tool to retrieve the list of agents.
If the MCP server is not available or the tool fails, fallback to using the `gcloud alpha agent-registry agents list --format="json"` command via the `run_shell_command` tool.
Once you have the data, format it as a clear Markdown table containing the agent's name, display name, location, and runtime. Ensure you present this table directly to the user.
"""
